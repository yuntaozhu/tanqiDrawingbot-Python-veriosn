# 📋 提示词融合 Bug 修复总结

**日期**: 2026-08-09  
**问题**: 多轮对话时，用户第2句话导致生成错误的画面  
**状态**: 🔴 待修复

---

## 问题描述

### 用户对话
```
用户: "查理王小猎犬和猫咪在打架"
AI:  "好的，为你绘制..." 
画面: [✅ 正确]

用户: "是的，把画面画出来"
画面: [❌ 与对话内容无关]
```

### 问题现象
- 第1句话生成的画面**正确**
- 第2句话 "是的，把画面画出来" 导致生成的画面**与原意不符**

---

## 根本原因

### 操作识别误判

**代码位置**: `src/operation_recognizer.py` 第 42 行

```python
modify_keywords = ["改成", "修改", "变成", "改色", "换成", "变", "改一下", "把"]
```

**问题**: 用户句子 "是的，把画面画出来" 中包含 **"把"** 字
- ❌ 被误识别为 **MODIFY（修改）** 操作
- ✅ 应该是 **CREATE（创建）** 操作

### 链式反应

```
识别为 MODIFY
    ↓
融合提示词: "基于已有画面...进行以下修改：是的，把画面画出来"
    ↓
这个提示词对 AI 绘画模型无意义
    ↓
LLM 扩展超时（10秒）
    ↓
降级到本地扩展（理解度差）
    ↓
生成错误的画面 ❌
```

### 为什么 "把" 是问题？

| 用法 | 例句 | 操作 | 是否应触发 MODIFY |
|-----|------|------|------------------|
| 介词（创建） | "把画面画出来" | CREATE | ❌ 否 |
| 介词（创建） | "把小狗画出来" | CREATE | ❌ 否 |
| 介词（修改） | "把小狗改成蓝色" | MODIFY | ✅ 是 |
| **结论** | **歧义大** | **不确定** | **❌ 应删除** |

---

## 修复方案概览

### 核心修复（4 个步骤）

| 步骤 | 文件 | 修改 | 时间 | 优先级 |
|-----|------|------|------|--------|
| 1️⃣ | operation_recognizer.py | 删除 "把" 关键字 | 2 min | 🔴 高 |
| 2️⃣ | prompt_fusion.py | 检查已有画面 | 3 min | 🟠 中 |
| 3️⃣ | operation_recognizer.py | 改进置信度校准 | 5 min | 🟠 中 |
| 4️⃣ | routes/device.py | 上下文感知降级 | 5 min | 🟡 低 |

---

## 修复前后对比

### 修复前 ❌

```
第1句: "查理王小猎犬和猫咪在打架"
  操作识别: CREATE ✅
  融合提示词: "查理王小猎犬和猫咪在打架" ✅
  生成结果: [正确的画面] ✅

第2句: "是的，把画面画出来"
  操作识别: MODIFY ❌ (因为"把")
  融合提示词: "基于已有画面...进行修改：是的，把画面画出来" ❌
  生成结果: [错误的画面] ❌
```

### 修复后 ✅

```
第1句: "查理王小猎犬和猫咪在打架"
  操作识别: CREATE ✅
  融合提示词: "查理王小猎犬和猫咪在打架" ✅
  生成结果: [正确的画面] ✅

第2句: "是的，把画面画出来"
  操作识别: MODIFY (初步) → 降级为 CREATE (最终) ✅
  融合提示词: "是的，把画面画出来" ✅
  生成结果: [正确的画面] ✅
```

---

## 关键代码修改

### 修改 1: operation_recognizer.py 第 42 行

```python
# 修改前 - 包含 "把"
modify_keywords = ["改成", "修改", "变成", "改色", "换成", "变", "改一下", "把"]

# 修改后 - 删除 "把" 和 "变"
modify_keywords = ["改成", "修改", "变成", "改色", "换成", "改一下"]
```

**为什么删除 "变"**:
- "变黑色" → MODIFY ✓
- "变成现实" → CREATE ✗
- 歧义性高，不删除

### 修改 2: prompt_fusion.py 第 47-50 行

```python
# 修改前 - 无条件进行 MODIFY 融合
elif operation_type == "modify":
    elements_str = ", ".join(scene_elements) if scene_elements else "已有元素"
    fused_prompt = f"基于包含 {elements_str} 的已有画面，...修改：{current_user_text}。"

# 修改后 - 检查是否有已有画面
elif operation_type == "modify":
    has_previous_context = previous_prompt is not None or (scene_elements and len(scene_elements) > 0)
    if not has_previous_context:
        fused_prompt = current_user_text  # 降级为 CREATE 的处理方式
    else:
        elements_str = ", ".join(scene_elements) if scene_elements else "已有元素"
        fused_prompt = f"基于包含 {elements_str} 的已有画面，...修改：{current_user_text}。"
```

---

## 验证清单

修复后需要验证：

- [ ] 部署成功，web 服务 🟢 Online
- [ ] 测试用例 1: "查理王小猎犬和猫咪在打架" + "是的，把画面画出来"
  - [ ] 第1句生成正确的画面
  - [ ] 第2句也生成正确的画面（而非错误的）
- [ ] 测试用例 2: "画一个红色小狗" + "把小狗改成蓝色"
  - [ ] 第1句生成红色小狗
  - [ ] 第2句修改为蓝色小狗
- [ ] 测试用例 3: "画一个小狗" + "再加一只猫咪"
  - [ ] 识别为 ADD 操作
  - [ ] 成功添加猫咪
- [ ] 部署日志中没有提示词融合的误判警告

---

## 部署步骤

1. **修改代码** (15 分钟)
   ```bash
   # 修改 4 个文件
   - src/operation_recognizer.py (2 处修改)
   - src/prompt_fusion.py (1 处修改)
   - src/routes/device.py (1 处修改)
   ```

2. **提交 PR** (5 分钟)
   ```bash
   git add .
   git commit -m "fix: improve operation recognition and prompt fusion logic"
   git push
   ```

3. **部署** (3-5 分钟)
   - Railway 自动检测并部署
   - 检查部署状态

4. **验证** (10 分钟)
   - 测试用户对话流程
   - 检查日志输出

---

## 风险评估

| 风险 | 等级 | 说明 | 缓解 |
|-----|------|------|------|
| 删除 "把" 关键字 | 🟢 低 | "把" 很少用于 MODIFY | 添加充分的注释 |
| 修改 MODIFY 逻辑 | 🟢 低 | 只在无上下文时降级 | 有完整的日志记录 |
| 上下文感知降级 | 🟢 低 | 有后备的 CREATE 操作 | 充分测试所有路径 |

**总体风险**: 🟢 **低** - 改进现有逻辑，无新算法

---

## 性能影响

- ⚡ 无性能下降（逻辑优化）
- ✅ 可能改进（避免 10s LLM 扩展超时）
- 📊 提高成功率（减少无关画面生成）

---

## 后续改进（可选）

1. **更好的操作识别**
   - 使用 NLP 模型而非关键字匹配
   - 考虑语法结构和上下文

2. **更智能的融合**
   - 基于 scene_elements 的相似度匹配
   - 多轮对话的智能组合

3. **更好的错误恢复**
   - 当提示词生成失败时的降级策略
   - 用户反馈的学习机制

---

## 相关文档

- **PROMPT_FUSION_BUG_ANALYSIS.md** - 详细的技术分析
- **FIX_PROMPT_FUSION_PROMPT.md** - AI 工程师修复提示词
- **src/operation_recognizer.py** - 待修改文件
- **src/prompt_fusion.py** - 待修改文件
- **src/routes/device.py** - 待修改文件

---

## 修复时间表

| 里程碑 | 时间 | 状态 |
|--------|------|------|
| 问题分析 | 2026-08-09 03:30 | ✅ 完成 |
| 文档编写 | 2026-08-09 03:45 | ✅ 完成 |
| 代码修改 | 2026-08-09 03:50-04:05 | ⏳ 待执行 |
| PR 提交 | 2026-08-09 04:10 | ⏳ 待执行 |
| 部署 | 2026-08-09 04:15 | ⏳ 待执行 |
| 验证 | 2026-08-09 04:25 | ⏳ 待执行 |

---

## 总结

🔴 **问题优先级**: 高 (影响核心功能)  
🟢 **修复复杂度**: 低 (逻辑调整)  
⚡ **修复时间**: 25 分钟  
✅ **风险等级**: 低 (充分测试)  

**建议**: 立即执行修复，提升用户体验

