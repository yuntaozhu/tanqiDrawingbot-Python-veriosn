# 探奇 AI 机器人项目：当前 API 调用与处理流程文档

本篇文档详细梳理了 **Toddler Drawing Dreamer (探奇智能对话与绘画机器人)** 服务端系统目前所集成的各项外部 AI 接口、API 密钥配置（Keys）、以及完整的语音/文本/图像调用与端到端多模态流转过程。

---

## 一、 核心 API 密钥配置 (API Keys)

系统中所有的 API 密钥和接口路由都配置在项目根目录的 `.env` 配置文件中。以下是重构后实际生效的配置表：

| 环境变量名称 | 配置的值 / 默认值 | 主要用途 | 对应使用的模型 / 接口 |
| :--- | :--- | :--- | :--- |
| **`ARK_API_KEY`** | 在部署环境中设置（不记录真实值） | 火山引擎豆包大模型授权凭证 | 豆包全系列多模态大模型授权 |
| **`ARK_AUDIO_MODEL`** | `doubao-seed-2-0-lite-260428` | 火山引擎端到端音频大模型，用于一键完成语音听写、心理特征提取与回复决策 | `doubao-seed-2-0-lite-260428` |
| **`ARK_DRAW_MODEL`** | `doubao-seedream-5-0-pro-260628` | 最新高精度 Seedream 5.0 pro 绘画创作引擎，专为儿童黑白简笔画定制 | `doubao-seedream-5-0-pro-260628` |
| **`DEEPSEEK_API_KEY`** | 在部署环境中设置（不记录真实值） | 备用大语言模型、中文文本理解及成长报告自动撰写 | `deepseek-chat` |
| **`STT_API_KEY`** | 在部署环境中设置（不记录真实值） | 备用语音转文字 (STT) 识别引擎 | `FunAudioLLM/SenseVoiceSmall` |
| **`TTS_API_KEY`** | 在部署环境中设置（不记录真实值） | 备用语音合成 (TTS) 外放引擎 | `tts-1` (Alloy 声音) |
| **`REPLICATE_API_TOKEN`** | 在部署环境中设置（不记录真实值） | 备用简笔画生成，大模型自动翻译 | `bytedance/seedream-4` |
| **`IDEOGRAM_API_KEY`** | 在部署环境中设置（不记录真实值） | 备用高分辨率简笔画生成 | `ideogram-v4` (`DEFAULT`) |

---

## 二、 核心功能调用过程与数据流转

系统的交互主要通过客户端上报的 **语音输入 (Voice - `/api/device/v1/voice`)** 或 **文本输入 (Chat - `/api/device/v1/chat`)** 触发。

### 1. 豆包端到端音频多模态智能流水线 (Unified Audio Pipeline)

当设备上报音频二进制数据（WAV 格式）时，服务端优先启用 **火山引擎豆包端到端音频大模型** 交互体系，执行高度整合的流转处理：

```
                [ 客户端 (设备) ]
                       │ (上传 WAV 录音二进制)
                       ▼
            [ 音频预处理 (Butterworth 带通滤波) ]
                       │
                       ▼
          [ 豆包端到端音频大模型 (ARK_AUDIO_MODEL) ]
                       │
        ┌──────────────┴──────────────┐
        ▼ (结构化分析)                 ▼ (对话回复文本)
 [ 心理认知丰富度指标 ]            [ 回复文本 & 意图分流 ]
        │                             │
        │                             ├──────────────────┐
        ▼                             ▼ (绘图需求 = true)  ▼ (纯聊天)
[ 豆包 Embedding 编码 ]       [ 豆包 Seedream 5.0 pro ]  [ SiliconFlow TTS ]
        │                             │                  (alloy亲切合金音)
        ▼                             ▼                          │
 [ SQLite 向量数据库 ]       [ OpenCV 1-bit 二值化 ]             │
 (PsychVectorDB 存储)                 │                          │
                                      ▼                          ▼
                            [ 1-bit 排包 Hex 码 ]         [ Base64 语音回传 ]
                                      │                          │
                                      ▼                          ▼
                            [ 自动创建热敏打印作业 ]  ───► [ 设备解码播放 ]
```

1. **多模态音频流转与指令识别**：
   - 原始音频数据经 `preprocess_audio` 带通滤波降噪后，直接作为 `input_audio` 部分发送至豆包端到端音频大模型。
   - 大模型在单个调用会话内，输出高质量、易于解析的结构化 **JSON 对象**，涵盖听写内容、探奇老师温柔回复，以及用于后期儿童成长测评的丰富心理特征数据。
2. **多指标早期行为心理特征提取**：
   - **detected_emotions**：智能捕捉儿童表达时的心理底色（如快乐、分离焦虑、好奇、愤怒、悲伤等）。
   - **linguistic_richness_score**：量化儿童句子逻辑度及词汇量（0.0 ~ 1.0），生成高维发展成长走势图。
   - **cognitive_milestone_ref**：参照“同理心萌芽”、“符号思维阶段”等前运算阶段科学标注。
   - **key_interests**：提取其关切的主题（小动物、童话、车辆等）。
   - **requires_attention**：当识别出极端焦虑、安全感匮乏、分离恐惧等需要干预的情绪时，自动标记为 `true`。
3. **心理特征向量检索与持久化**：
   - 聊天文本与回复由 `doubao-embedding-vision-240528` （vision-text 联合编码，备用 `doubao-embedding-text-240715`）编码为高维密集浮点向量。
   - 连同成长指标、时间戳，通过 ORM 持久化写入 SQLite 的 `psych_vectors` 向量数据库。

---

### 2. 豆包 Seedream 5.0 pro 黑白二值简笔画过滤

一旦豆包端到端模型检测到绘画意图：

1. **高精度简笔画出图**：
   - 调用最新的 **Doubao Seedream 5.0 pro** 接口进行创意生图。系统自动装配了针对微型热敏纸专门优化的国风及矢量装饰性中文 Prompt：
     > *"极简黑白儿童简笔画，一个可爱卡通的{prompt}，高对比度，纯白背景，纯黑线条，1-bit 扁平矢量线稿，无渐变，无阴影，居中，极简美学，适合热敏纸打印。CRITICAL: NO TEXT, NO ENGLISH WORDS."*
   - 若火山大模型连接因欠费或信道受限，系统将在重试避让后一键自动 fallback 到 Replicate (seedream-4) 或 Ideogram。
2. **OpenCV 1-Bit 位图包压缩**：
   - 提取生成的图像 URL，使用 OpenCV 在服务端缓存重采样为 320x320 尺寸，执行严格的动态阈值二值化（Mode = '1'）。
   - 通过 `img.tobytes().hex()` 压缩为紧凑的 packed-bits 16进制格式，生成完全符合微控制器（如 ESP32）和 Type-C 串口热敏打印机直接读取的高性能包，作为异步待打印任务加入 SQL 打印队列中。

---

### 3. 家长端“早期心理成长与行为诊断报告”

家长或教师通过访问 `/api/admin/reports/{device_token}`，可实时渲染并打印出极其专业温暖的成长分析：

1. **指标聚合与趋势 SVG 渲染**：
   - 服务端提取该设备专属的高维度心理向量数据，计算近期十次对话的平均言语表达组织丰富度。
   - 采用原生高性能 HTML + Tailwind CSS，配合轻量级动态 SVG 曲线和数据悬停微动画，展示言语发展变化。
2. **多标签情绪及兴趣图谱**：
   - 对识别到的心理分类和关注主题执行降序频率排名，辅以危险预警条目高亮（对频繁分离焦虑和极度缺乏安全感予以重点醒目提示）。
3. **专业级成长与家园共育意见书**：
   - 汇总近期所有的儿童聊天原话和心理标签，将其通过火山/DeepSeek 处理。
   - 基于皮亚杰早期认知发展理论、埃里克森心理社会发展阶段，动态生成专业级的“行为诊断分析”，并输出 3 条切实可行的具体亲子家园互动意见，给予早期无扰的积极关怀支撑。
